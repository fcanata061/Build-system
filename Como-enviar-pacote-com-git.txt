git add "${PKG}-${VER}-${REL}.tar.xz" "package.json"
git commit -m "Add ${PKG} ${VER}-${REL}"
git tag -a "${PKG}-${VER}-${REL}" -m "Release ${PKG} ${VER}-${REL}"
git push --follow-tags

E com RSYNC
rsync -avz --progress \
  "${PKG_CACHE_DIR}/${PKG}-${VER}-${REL}.tar.xz" \
  "${REMOTE_REPO}/${PKG}/"
